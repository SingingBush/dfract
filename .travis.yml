# travis now supports D and will run 'dub test' with multiple compilers
language: d

sudo: required

services:
 - docker

d:
 - dmd
# - gdc  won't work with gdc
 - ldc

os:
 - linux
 - osx

matrix:
  allow_failures:
    - os: osx
